package com.api.payments.config;

public class TokenAuthenticationService {

//        Date dateNow = new Date();
//        String pattern = "yyyy-MM-dd'T'HH:mm:ss.SSS";
//        String date = new SimpleDateFormat(pattern).format(dateNow);

//        HttpClient client = HttpClient.newHttpClient();
//
//        HttpRequest request = HttpRequest.newBuilder()
//            .uri(URI.create(this.WHATSAPP_URL))
////            .header("Authorization", WHATSAPP_PERMANENT_TOKEN)
//            .header("accept", "application/json")
////            .header("key2", "value2")
//            .timeout(Duration.of(30, SECONDS))
//            .GET()
//            .build();
//
//        {
//            HttpResponse<String> response;
//
//            try {
//                response = client.send(request, HttpResponse.BodyHandlers.ofString());
//                System.out.println("HttpResponse: " + response.body());
//            } catch (IOException | InterruptedException e) {
//                throw new RuntimeException(e);
//            }
//            return response;
//        }
}
